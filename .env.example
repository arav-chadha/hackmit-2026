ELASTIC_URL=
ELASTIC_API_KEY=

# Optional
# OWNER_NAME=you                                  display name of the account owner in the export
# ELASTIC_INDEX_PREFIX=arav-                      keeps teammates' data apart on a shared cluster
# ELASTIC_INFERENCE_ID=.elser-2-elastic     embedding model for semantic search

ANTHROPIC_API_KEY=
# JUDGE_MODEL=claude-haiku-4-5                      reads candidates and decides; many cheap calls
# WRITER_MODEL=claude-opus-5                        writes card text and drafts; few calls
